# 機能詳細

## 主要機能

### 1. To Doリスト管理

#### データ項目
- **タスク名**: タスクの内容を記入
- **担当者メールアドレス**: リマインド送信先
- **締切日**: タスクの期限
- **ステータス**: 未着手 / 進行中 / 完了 / 保留
- **備考**: 追加情報やメモ

#### 自動フォーマット機能
- ヘッダー行の色付け（青背景 + 白文字）
- 列幅の自動調整
- 日付フォーマットの自動適用
- ステータスのドロップダウンリスト
- セルの枠線自動設定

### 2. リマインドメール機能

#### リマインドタイミング
デフォルトでは以下のタイミングでメールを送信：

| タイミング | メール件名の例 |
|-----------|---------------|
| 締切3日前 | 【リマインド】タスク締切まであと3日 |
| 締切1日前 | 【リマインド】タスク締切は明日です |
| 締切当日 | 【重要】タスク締切は本日です |
| 締切1日後 | 【緊急】タスクの締切を過ぎています |

#### メール内容
- 緊急度に応じた件名
- タスクの詳細情報
- 締切日の日本語表記
- 現在のステータス
- 備考欄の内容
- スプレッドシートへの直接リンク

#### 送信条件
- ステータスが「完了」のタスクには送信しない
- 必須項目（タスク名、担当者、締切日）が入力されている
- 締切日が設定タイミングに該当する

### 3. 自動実行トリガー

#### トリガーの種類
- **時間主導型トリガー**: 毎日指定時刻に自動実行
- デフォルト設定: 毎日午前8時

#### トリガー管理機能
- `createDailyTrigger`: トリガーの作成
- `deleteDailyTrigger`: トリガーの削除
- `showTriggers`: 現在のトリガー確認
- 重複トリガーの自動防止

### 4. テスト・デバッグ機能

#### 利用可能な関数
- **setupSheet**: 初期設定とサンプルデータ作成
- **testReminders**: リマインド機能の即座テスト
- **showTriggers**: トリガー設定の確認
- **sendReminders**: メインのリマインド処理（手動実行可能）

#### ログ機能
- メール送信の成功/失敗ログ
- トリガー実行履歴
- エラーメッセージの記録

## 技術仕様

### 使用技術
- **Google Apps Script (GAS)**: メイン実行環境
- **Googleスプレッドシート API**: データ管理
- **MailApp（Apps Scriptの標準サービス）**: メール送信
- **Time-based Triggers**: 自動実行

### データ処理
- 日付計算: ミリ秒単位での精密計算
- タイムゾーン対応: `Session.getScriptTimeZone()`使用
- データバリデーション: 空データや完了タスクのスキップ

### セキュリティ
- **必要な権限**:
  - スプレッドシートの読み取りと編集
  - メールの送信
  - 時間主導型トリガーの作成

### パフォーマンス
- **メール送信制限**: 1日約100通（Googleアカウントの制限）
- **実行時間**: 通常1-2分以内
- **データ処理**: 1000行程度まで問題なく処理可能

## カスタマイズ可能な項目

### 設定変更可能項目

#### 1. リマインド日数
```javascript
const REMINDER_DAYS = [3, 1, 0, -1];
```
変更例:
- 1週間前追加: `[7, 3, 1, 0, -1]`
- 当日のみ: `[0]`

#### 2. 実行時刻
```javascript
.atHour(8) // 0-23で指定
```

#### 3. メール件名・本文
`sendReminderEmail` 関数内で自由に編集可能

#### 4. ステータスの選択肢
```javascript
.requireValueInList(['未着手', '進行中', '完了', '保留'], true)
```

#### 5. 列の追加
`COLUMNS` オブジェクトに列を追加して拡張可能

### 拡張アイデア

#### 実装可能な機能
1. **優先度の追加**
   - 高/中/低の優先度設定
   - 優先度による色分け

2. **カテゴリー分類**
   - プロジェクト別のタスク管理
   - カテゴリーフィルター

3. **進捗率の追加**
   - パーセンテージでの進捗管理
   - 進捗グラフの表示

4. **複数担当者対応**
   - カンマ区切りで複数メールアドレス
   - 全員にリマインド送信

5. **Slackとの連携**
   - Slack Webhook経由で通知
   - チャンネルへの投稿

6. **完了率のダッシュボード**
   - 別シートにグラフ表示
   - 統計情報の自動集計

## 制限事項

### Google Apps Scriptの制限
- 1日のメール送信: 約100通
- 1回の実行時間: 最大6分
- トリガー実行回数: 1日90回まで

### 推奨事項
- タスク数: 100件程度まで
- 同時送信メール: 50通程度まで
- 実行頻度: 1日1回が推奨

## サポートブラウザ

- Google Chrome（推奨）
- Firefox
- Safari
- Microsoft Edge
- モバイルブラウザ（閲覧・編集）

## ライセンス

MIT License

このプロジェクトはオープンソースです。自由に改変・再配布してください。

## クレジット

開発: Google Apps Script
UI: Google スプレッドシート
メール送信: MailApp（Apps Script）
