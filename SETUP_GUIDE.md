# Google スプレッドシート To Do リスト - 詳細セットアップガイド

## 目次

1. [前提条件](#前提条件)
2. [ステップ1: Googleスプレッドシートの作成](#ステップ1-googleスプレッドシートの作成)
3. [ステップ2: Google Apps Scriptの設定](#ステップ2-google-apps-scriptの設定)
4. [ステップ3: 初期設定の実行](#ステップ3-初期設定の実行)
5. [ステップ4: 権限の承認](#ステップ4-権限の承認)
6. [ステップ5: トリガーの設定](#ステップ5-トリガーの設定)
7. [ステップ6: テスト実行](#ステップ6-テスト実行)
8. [使い方](#使い方)
9. [カスタマイズ方法](#カスタマイズ方法)
10. [トラブルシューティング](#トラブルシューティング)

---

## 前提条件

- Googleアカウント
- Gmailアドレス（リマインドメール受信用）
- インターネット接続

---

## ステップ1: Googleスプレッドシートの作成

### 1-1. 新規スプレッドシートを作成

1. Googleドライブ (https://drive.google.com) にアクセス
2. 左上の「新規」ボタンをクリック
3. 「Googleスプレッドシート」→「空白のスプレッドシート」を選択

### 1-2. ファイル名を設定

- 左上のファイル名をクリックして「To Doリスト」などの名前に変更

---

## ステップ2: Google Apps Scriptの設定

### 2-1. スクリプトエディタを開く

1. 作成したスプレッドシートを開いた状態で
2. メニューバーから「拡張機能」をクリック
3. 「Apps Script」を選択

### 2-2. コードをコピー＆ペースト

1. Apps Scriptエディタが新しいタブで開きます
2. 既存のコード（`function myFunction() { ... }`）を全て削除
3. このプロジェクトの `Code.gs` ファイルの内容を全てコピー
4. Apps Scriptエディタに貼り付け
5. 左上の「プロジェクトを保存」（💾アイコン）をクリック
6. プロジェクト名を入力（例: "To Doリマインダー"）

---

## ステップ3: 初期設定の実行

### 3-1. setupSheet関数を実行

1. Apps Scriptエディタの上部にある関数選択ドロップダウンをクリック
2. 「setupSheet」を選択
3. 「実行」ボタン（▶️再生マークのアイコン）をクリック

### 3-2. 実行結果の確認

1. 実行が完了すると「設定完了」のアラートが表示されます
2. スプレッドシートのタブに戻ると、以下が自動的に作成されています：
   - ヘッダー行（青い背景）
   - サンプルデータ3件
   - 列幅の調整
   - ステータスのドロップダウンリスト

---

## ステップ4: 権限の承認

### 4-1. 初回実行時の権限承認

初めて関数を実行すると、以下の手順で権限の承認が必要です：

1. 「承認が必要です」というダイアログが表示されます
2. 「権限を確認」ボタンをクリック
3. Googleアカウント選択画面で、使用するアカウントを選択
4. 「このアプリは確認されていません」という警告が表示される場合：
   - 「詳細」をクリック
   - 「（プロジェクト名）に移動」リンクをクリック
5. 権限の確認画面で以下の権限を確認：
   - スプレッドシートの表示と管理
   - メールの送信
6. 「許可」ボタンをクリック

### 4-2. 権限の説明

このスクリプトが要求する権限：

| 権限 | 理由 |
|------|------|
| スプレッドシートの表示と管理 | To Doリストのデータを読み取るため |
| 外部サービスへの接続 | Gmailでメールを送信するため |
| このアプリケーションがユーザー不在時でも実行される許可 | 自動トリガーでリマインドを送信するため |

---

## ステップ5: トリガーの設定

### 5-1. 自動実行トリガーの作成

1. Apps Scriptエディタに戻る
2. 関数選択ドロップダウンから「createDailyTrigger」を選択
3. 「実行」ボタンをクリック
4. 「トリガー設定完了」のアラートが表示されれば成功

### 5-2. トリガーの確認

1. Apps Scriptエディタの左サイドバーで「トリガー」（⏰時計アイコン）をクリック
2. 以下のトリガーが表示されていることを確認：
   - 関数: `sendReminders`
   - イベントソース: 時間主導型
   - 時刻ベースのトリガー: 日タイマー
   - 時刻: 午前8時～9時

---

## ステップ6: テスト実行

### 6-1. サンプルデータの修正

1. スプレッドシートに戻る
2. サンプルデータの「担当者メールアドレス」列を自分のメールアドレスに変更
3. 締切日を以下のように変更してテスト：
   - 1行目: 2日後の日付（リマインド対象外）
   - 2行目: 3日後の日付（3日前リマインド）
   - 3行目: 明日の日付（1日前リマインド）

### 6-2. 手動でテスト実行

1. Apps Scriptエディタに戻る
2. 関数選択ドロップダウンから「testReminders」を選択
3. 「実行」ボタンをクリック
4. 実行完了後、Gmailをチェック

### 6-3. 実行ログの確認

1. Apps Scriptエディタの左サイドバーで「実行数」をクリック
2. 最新の実行結果を確認
3. エラーがないことを確認

---

## 使い方

### To Doの追加

1. スプレッドシートの新しい行に以下を入力：

| 列 | 入力内容 | 例 |
|---|---|---|
| A: タスク名 | タスクの名前 | 「プレゼン資料作成」 |
| B: 担当者メールアドレス | 通知先のメールアドレス | 「tanaka@example.com」 |
| C: 締切日 | 締切の日付 | 「2024/01/25」 |
| D: ステータス | ドロップダウンから選択 | 「未着手」 |
| E: 備考 | メモ（任意） | 「重要案件」 |

### To Doの更新

- **進捗管理**: ステータス列をクリックして、ドロップダウンから状態を選択
  - 未着手
  - 進行中
  - 完了（リマインドが停止します）
  - 保留

### リマインドメールの内容

受信するメールには以下の情報が含まれます：

```
【リマインド】タスク締切まであと3日

締切まであと3日です。

━━━━━━━━━━━━━━━━━━━━━━━━
【タスク情報】
━━━━━━━━━━━━━━━━━━━━━━━━

タスク名: プレゼン資料作成
締切日: 2024年01月25日
現在のステータス: 進行中
備考: 重要案件

━━━━━━━━━━━━━━━━━━━━━━━━

スプレッドシートで確認・更新:
https://docs.google.com/spreadsheets/d/...
```

---

## カスタマイズ方法

### リマインドのタイミングを変更

`Code.gs` の以下の部分を編集：

```javascript
const REMINDER_DAYS = [3, 1, 0, -1]; // 3日前、1日前、当日、1日後
```

例：
- 1週間前も追加: `[7, 3, 1, 0, -1]`
- 当日のみ: `[0]`
- 締切後は不要: `[3, 1, 0]`

### トリガーの実行時刻を変更

`Code.gs` の `createDailyTrigger` 関数内：

```javascript
.atHour(8) // 8時 → 好きな時刻（0-23）に変更
```

例：
- 朝7時: `.atHour(7)`
- 夜8時: `.atHour(20)`

変更後、再度 `createDailyTrigger` を実行してください。

### メール本文をカスタマイズ

`Code.gs` の `sendReminderEmail` 関数内の `body` 変数を編集：

```javascript
const body = `
ここにお好きなメッセージを入力
タスク名: ${taskName}
締切日: ${formattedDueDate}
`;
```

### ステータスの選択肢を変更

`Code.gs` の `setupSheet` 関数内：

```javascript
.requireValueInList(['未着手', '進行中', '完了', '保留'], true)
// お好きな選択肢に変更
.requireValueInList(['ToDo', 'Doing', 'Done'], true)
```

変更後、再度 `setupSheet` を実行してください。

---

## トラブルシューティング

### メールが送信されない

#### チェック項目1: メールアドレスの確認
- 担当者列にメールアドレスが正しく入力されているか
- 誤字や空白がないか確認

#### チェック項目2: トリガーの確認
1. Apps Scriptエディタの「トリガー」を確認
2. `sendReminders` のトリガーが存在するか
3. 存在しない場合、`createDailyTrigger` を再実行

#### チェック項目3: 締切日の確認
- 締切日が正しく入力されているか
- リマインド対象の日数（3日前、1日前など）に該当するか

#### チェック項目4: ステータスの確認
- ステータスが「完了」になっていないか確認
- 「完了」のタスクにはリマインドが送信されません

#### チェック項目5: 実行ログの確認
1. Apps Scriptエディタの「実行数」をクリック
2. エラーメッセージがないか確認
3. エラーがある場合、内容を確認して対処

### 権限エラーが出る

#### 解決方法1: 権限の再承認
1. Apps Scriptエディタで `setupSheet` を再実行
2. 権限承認の手順をもう一度実行

#### 解決方法2: Googleアカウントのセキュリティ設定
1. Googleアカウントのセキュリティ設定を確認
2. 「安全性の低いアプリのアクセス」が無効になっていないか確認
   - 最近のGoogleアカウントではこの設定は不要です

### トリガーが実行されない

#### 確認方法
1. Apps Scriptエディタの「実行数」で履歴を確認
2. トリガーが設定時刻に実行されているか確認

#### 解決方法
1. トリガーを削除して再作成
   - `deleteDailyTrigger` を実行
   - `createDailyTrigger` を実行
2. 時刻を変更してテスト

### スクリプトエラーが出る

#### よくあるエラーと対処法

**エラー: "TypeError: Cannot read property '0' of undefined"**
- 原因: スプレッドシートにデータが存在しない
- 解決: `setupSheet` を実行してサンプルデータを作成

**エラー: "ReferenceError: SpreadsheetApp is not defined"**
- 原因: コードが正しく貼り付けられていない
- 解決: `Code.gs` の内容を全て貼り直す

**エラー: "Authorization is required"**
- 原因: 権限が承認されていない
- 解決: 権限承認の手順を再度実行

### メールが迷惑メールフォルダに入る

#### 対処法
1. Gmailの迷惑メールフォルダを確認
2. メールを開いて「迷惑メールではない」をクリック
3. フィルタを作成して受信トレイに振り分け

### 複数人で共有したい

#### 手順
1. スプレッドシートを共有
   - 「共有」ボタンをクリック
   - メンバーのメールアドレスを追加
   - 「編集者」権限を付与
2. 各メンバーが個別にトリガーを設定
   - 各自のGoogleアカウントで `createDailyTrigger` を実行
   - これにより、各自のアカウントからメールが送信されます

---

## よくある質問

### Q1: リマインドは誰に送信されますか？
A: 各行の「担当者メールアドレス」列に入力されたメールアドレスに送信されます。

### Q2: 1日に何通までメールを送信できますか？
A: Googleアカウントの制限により、1日約100通までです。それ以上必要な場合は、Google Workspace（有料版）の利用を検討してください。

### Q3: トリガーを停止したい場合は？
A: `deleteDailyTrigger` 関数を実行するか、Apps Scriptエディタの「トリガー」からトリガーを削除してください。

### Q4: 複数のスプレッドシートで使えますか？
A: はい。各スプレッドシートに個別にスクリプトを設定してください。

### Q5: スマートフォンでも使えますか？
A: はい。GoogleスプレッドシートのモバイルアプリでTo Doの追加・編集が可能です。リマインドメールもスマートフォンで受信できます。

---

## サポート

問題が解決しない場合は、以下を確認してください：

1. Google Apps Scriptの公式ドキュメント
   - https://developers.google.com/apps-script
2. Googleスプレッドシートのヘルプセンター
   - https://support.google.com/docs

---

## 更新履歴

- 2024/XX/XX: 初版作成
  - 基本機能の実装
  - リマインドメール機能
  - 自動トリガー設定

---

このガイドを参考に、快適なTo Do管理をお楽しみください！
